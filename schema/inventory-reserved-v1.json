{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title" : "InventoryReservedEvent",
  "type" : "order",
  "properties": {
    "eventId": { "type" :  "string"},
    "orderId": {"type" : "string"},
    "status" : {"type" : "string"},
    "reason" : {"type" :  "string"},
    "createdAt" : {"type" : "string", "format" : "date-time"}
  },
  "required": ["eventId", "orderId", "status", "createdAt"],
  "additionalProperties": false
}